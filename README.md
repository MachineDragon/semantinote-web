# SemantiNote website

The marketing + download site (static HTML/CSS/JS — hosts free on GitHub Pages).

**Everything you need to build, publish, sell, and market SemantiNote is in one guide:**
👉 **`../semantinote-app/START-HERE.md`**

- `index.html`, `features.html`, `download.html`, `thanks.html` — the pages
- `assets/` — styles, JS, logo, screenshots
- `license-worker/` — the free Cloudflare Worker that issues + checks product keys
- `.nojekyll` — makes GitHub Pages serve the files as-is (keep it)
